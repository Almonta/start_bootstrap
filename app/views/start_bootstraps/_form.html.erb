<%= form_with(model: start_bootstrap, local: true) do |form| %>
  <% if start_bootstrap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(start_bootstrap.errors.count, "error") %> prohibited this start_bootstrap from being saved:</h2>

      <ul>
      <% start_bootstrap.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
